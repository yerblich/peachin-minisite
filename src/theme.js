import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#1A394C"
        },
        secondary: {
            main:"#EF8F53"
        }
    },
    background: {
        default: "#fff"
    },
    spacing: 8,

})

export default theme;